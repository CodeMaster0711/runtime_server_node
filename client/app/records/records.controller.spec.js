'use strict';

describe('Component: RecordController', function () {

  // load the controller's module
  // beforeEach(module('testProjectApp'));

  // var RecordController, scope;

  // // Initialize the controller and a mock scope
  // beforeEach(inject(function ($componentController, $rootScope) {
  //   scope = $rootScope.$new();
  //   RecordController = $componentController('RecordController', {
  //     $scope: scope
  //   });
  // }));

  // it('should ...', function () {
  //   expect(1).toEqual(1);
  // });
});
